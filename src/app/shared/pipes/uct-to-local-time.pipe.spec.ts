import { UctToLocalTimePipe } from './uct-to-local-time.pipe';

describe('UctToLocalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new UctToLocalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
